# Changelog

gogoducky 私域代购周刊货架(加拿大→中国)。

## [0.2.0.0] - 2026-07-27

### Added
- 云端管理后台(`/admin`,手机可用,Cloudflare Access 邮箱验证码登录):改已发布期刊的文案与价格、延锁价、下架某期、发码与退役邀请码、看归因报表(按码 7/28 天、按期)
- 后台改动经 GitHub 提交后站点自动重建,提示明确说明"重建完成前旧版本仍在线",重要改动需回站点确认
- 期刊列表并发拉取并限量,报表三条查询合并为一次数据库往返

### Security
- 后台自带鉴权,不只依赖边缘:每个请求用 Cloudflare 的公钥验 Access 令牌签名,并校验受众/签发方/过期时间;自定义域之外的地址(包括平台默认域名与分支预览域,那里没有登录保护)一律拒绝
- 本地调试开关只在本地生效,在云端面板误开也解锁不了线上
- 云端编辑的全部文案先过药品关键词筛查(与选品管道、站点构建同一份清单),站点构建层再筛一遍
- 云端只能下架不能上架;改商品名会作废该商品的人工合规勾选并自动下架本期,必须回本地重过检查单
- 价格不得低于该商品的加币进价折算;锁价日期只能在发布日后有限天数内顺延
- 邀请码语法前后端统一校验,保留名(如 `__proto__`)拒绝

## [0.1.0.0] - 2026-07-26

### Added
- 「本周好货」周刊 H5 站点:每期商品清单、数量选择、一键复制订单文本(自动带期号/邀请码/锁价日),微信剪贴板失败自动降级为长按复制框;固定期链接与往期存档页,老刊自动进入存档态(锁价过期后禁下单、商品仍可浏览),发布超两周页首提示价格可能变动并引导往期存档
- 邀请码归因:专属链接全站透传邀请码,打开/复制事件写入 Cloudflare D1;白名单构建注入,退役码事件自动降级计入 direct 不丢流量;IP 只存时段盐哈希用于限频,原始 IP 永不落库
- 选品池爬虫管道:homarket.ca 分页抓全、价格解析为分单位整数(不确定的价格标记而不猜)、变价与下架检测(仅完整批次参与判定,连续两轮完整缺席才标下架)、并发批次锁
- 药品/器械关键词黑名单入池拦截,关键词与站点构建层共用同一清单;手工拉黑单向粘性,不因重复抓取洗白
- 一键入刊工具:选品生成期草稿、图片自动压缩 webp(≤100KB,限宽 800px)入仓自托管、定价公式(按当日汇率 + 服务费率 + 运费分摊)与毛利率兜底、汇率打错位保护、覆盖保护(重跑需 --force)
- 发布检查单:逐品强制勾选「非药品/非器械/无疗效宣称」,勾不全阻断发布;全部手填文案(卷首语/规格/时效/备注/角标)先过关键词机械筛查;站点构建层对已发布内容二次重筛,盖章后改文案命中关键词会被构建拦下(非关键词式表述的哈希绑定见 TODOS T-COMPLIANCE-HASH)
- 生产上线防线:演示码、占位微信号、占位二维码、未填的 D1 database_id 任一存在时生产构建直接失败
- 微信真机发版检查单与指标基线记录模板(docs/)
- 全套测试:管道 pytest、站点与归因端点 Vitest、Playwright 全链路 E2E(含本地 D1 真实落库回读)

### Security
- 凭据迁至环境变量,数据库/虚拟环境等敏感文件全部 gitignore
- 商品名(第三方抓取内容)注入页面前转义,阻断脚本逃逸;URL 邀请码过字符集闸,防伪造链接向订单文本夹带话术
- 商品图下载限制协议与体积,防解压炸弹
