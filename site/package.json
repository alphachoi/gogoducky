{
  "name": "gogoducky-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/gen-manifest.mjs && astro build",
    "preview": "astro preview",
    "test": "node scripts/gen-manifest.mjs && vitest run",
    "e2e": "playwright test",
    "e2e:server": "node scripts/e2e-server.mjs"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.0.0"
  }
}
